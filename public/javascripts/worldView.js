(function() {
  window.Physics = window.Physics || {};

  var WorldView = window.Physics.WorldView = function(world, ctx) {
    this.world = world;
    this.ctx = ctx;
  };

  WorldView.prototype.draw = function() {
  };

  WorldView.prototype.start = function() {
  };

  WorldView.prototype.stop = function() {
  };
})();
